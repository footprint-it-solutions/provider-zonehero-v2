---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: heroloadbalancerlistenerattachments.hlb.footprintit.net
spec:
  group: hlb.footprintit.net
  names:
    categories:
    - crossplane
    - managed
    - zonehero-v2
    kind: HeroLoadBalancerListenerAttachment
    listKind: HeroLoadBalancerListenerAttachmentList
    plural: heroloadbalancerlistenerattachments
    singular: heroloadbalancerlistenerattachment
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HeroLoadBalancerListenerAttachment is the Schema for the HeroLoadBalancerListenerAttachments
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HeroLoadBalancerListenerAttachmentSpec defines the desired
              state of HeroLoadBalancerListenerAttachment
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.loadBalancerId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loadBalancerId)
                || (has(self.initProvider) && has(self.initProvider.loadBalancerId))'
            - message: spec.forProvider.port is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.port)
                || (has(self.initProvider) && has(self.initProvider.port))'
            - message: spec.forProvider.protocol is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocol)
                || (has(self.initProvider) && has(self.initProvider.protocol))'
            - message: spec.forProvider.targetGroupArn is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.targetGroupArn)
                || (has(self.initProvider) && has(self.initProvider.targetGroupArn))'
          status:
            description: HeroLoadBalancerListenerAttachmentStatus defines the observed
              state of HeroLoadBalancerListenerAttachment.
            properties:
              atProvider:
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  id:
                    type: string
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: HeroLoadBalancerListenerAttachment is the Schema for the HeroLoadBalancerListenerAttachments
          API. <no value>
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: HeroLoadBalancerListenerAttachmentSpec defines the desired
              state of HeroLoadBalancerListenerAttachment
            properties:
              forProvider:
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.loadBalancerId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.loadBalancerId)
                || (has(self.initProvider) && has(self.initProvider.loadBalancerId))'
            - message: spec.forProvider.port is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.port)
                || (has(self.initProvider) && has(self.initProvider.port))'
            - message: spec.forProvider.protocol is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.protocol)
                || (has(self.initProvider) && has(self.initProvider.protocol))'
            - message: spec.forProvider.targetGroupArn is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.targetGroupArn)
                || (has(self.initProvider) && has(self.initProvider.targetGroupArn))'
          status:
            description: HeroLoadBalancerListenerAttachmentStatus defines the observed
              state of HeroLoadBalancerListenerAttachment.
            properties:
              atProvider:
                properties:
                  alpnPolicy:
                    description: |-
                      The Application-Layer Protocol Negotiation (ALPN) policy to use. Valid values:

                      * `HTTP1Only` - Only allow HTTP/1.* connections
                      * `HTTP2Only` - Only allow HTTP/2 connections
                      * `HTTP2Optional` - Prefer HTTP/1.*, but allow HTTP/2 if client supports it
                      * `HTTP2Preferred` - Prefer HTTP/2, but allow HTTP/1.* if client doesn't support HTTP/2
                      * `None` - Do not use ALPN
                    type: string
                  certificateSecretsName:
                    description: The name of the secret in AWS Secrets Manager containing
                      the SSL server certificate. This field is required if the protocol
                      is HTTPS. The secret should contain the certificate and private
                      key in PEM format.
                    type: string
                  enableDeletionProtection:
                    description: If true, deletion of the listener will be disabled
                      via the API. This prevents accidental deletion of production
                      listeners. You must set this to false and apply the change before
                      you can delete the listener.
                    type: boolean
                  id:
                    type: string
                  loadBalancerId:
                    description: The ID of the HLB load balancer to attach this listener
                      to. This cannot be changed once the listener is created - you
                      must create a new listener to move it to a different load balancer.
                    type: string
                  overprovisioningFactor:
                    description: When the load balancer `enable_cross_zone_load_balancing`
                      is set to `avoid` (default), this factor determines how much
                      traffic a single instance can receive relative to the average.
                      For example, with a value of `1.1`, an instance can receive
                      up to 110% of the average traffic. This helps optimize resource
                      usage while maintaining zone isolation. Must be greater than
                      or equal to 1.0. Applies to the target group - behavior is undefined
                      if different values are assigned to different listeners with
                      the same target group.
                    type: number
                  port:
                    description: |-
                      The port on which the load balancer is listening. Must be between 1 and 65535. Common ports:

                      * 80 - HTTP
                      * 443 - HTTPS

                      Note: The security groups associated with the load balancer must allow inbound traffic on this port.
                    type: number
                  protocol:
                    description: |-
                      The protocol for connections from clients to the load balancer. Valid values:

                      * `HTTP` - For unencrypted HTTP traffic
                      * `HTTPS` - For encrypted HTTPS traffic. When using HTTPS, you must also specify a `certificate_secrets_name`

                      * `UDP` - For raw unencrypted UDP traffic

                      The protocol cannot be changed after the listener is created - you must create a new listener to change protocols.
                    type: string
                  targetGroupArn:
                    description: The Amazon Resource Name (ARN) of the target group
                      to route traffic to. The target group defines where traffic
                      will be sent (the backend instances, IP addresses, or other
                      resources) and how health checks are performed. The target group
                      must be in the same region as the load balancer.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: false
